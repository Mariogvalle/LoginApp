<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.yourcompany.loginapp" android:versionCode="1" android:versionName="1">
	<!-- Permissions should be placed here -->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:supportsRtl="true" android:theme="@style/Theme.AppCompat" android:label="LoginApp">
		<activity android:name="@android:string/yes" android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.VIEW" />
				<category android:name="android.intent.category.DEFAULT" />
				<category android:name="android.intent.category.BROWSABLE" />
				<data android:scheme="com.yourcompany.loginapp" android:host="oauth2redirect" />
			</intent-filter>
		</activity>
		<!-- Other activities, services, or receivers can be added here -->
	</application>
</manifest>